# Core: the Google Gen AI SDK powers the agent runtime and function calling.
google-genai>=2.14.0

# Web server
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # multipart/form-data (file uploads)

# Tooling
python-docx>=1.1.0        # resume document generation
python-dotenv>=1.0.0      # .env configuration loading
pypdf>=4.0.0              # reading PDF resumes on upload
